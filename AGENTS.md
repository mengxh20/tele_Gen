# AGENTS.md

## 项目概览
- 项目名称：高效视频传输与重建
- 基础来源：本项目基于开源视频生成模型 Helios 进行二次开发
- 当前核心方向：从通用视频生成能力中抽取并强化“视频重建”能力，做成生成式视频重建系统
- 目标价值：利用生成模型低码率表达能力，实现低码率视频压缩与高质量重建

## 当前第一优先级
- 优先服务“视频重建”业务，而不是泛化地增强 t2v / i2v / v2v 生成效果
- 所有训练、推理、数据处理和评测相关改动，都应优先回答以下问题：
  - 是否有助于低码率压缩与重建
  - 是否有助于提升重建质量、时序一致性、码率效率或训练稳定性
  - 是否能复用现有生成模型能力来服务重建任务

## 关键入口
- 训练启动脚本：`scripts/training/train_ddp.sh`
- 训练主程序：`train_helios.py`
- 视频重建测试脚本：`new/recover_video.sh`

## 相关目录理解
- `scripts/inference/`：现有开源能力入口，包含 `t2v`、`i2v`、`v2v` 三类视频生成脚本
- `scripts/training/`：训练脚本与配置
- `new/`：与当前视频重建、可视化、实验验证更相关的新增逻辑
- `helios/`：核心模型、pipeline、scheduler、dataset、utils 等实现

## 业务理解
- 该项目不是单纯做视频生成，而是要把视频生成模型改造成“生成式视频重建模型”
- “低码率压缩与重建”是主线，应重点关注：
  - latent 表达是否适合压缩
  - 编码信息是否足以支撑高保真重建
  - 重建视频的时序连续性、清晰度、细节恢复能力
  - 训练与推理链路是否贴合真实压缩-重建场景
- 若通用生成能力改动与视频重建目标冲突，应优先选择视频重建目标

## 协作要求
- 每次接到需求时，先解释方案，再改代码
- 输出时必须标注：
  - 影响文件
  - 回滚方式
- 默认优先做最小必要修改，避免无关重构
- 若发现需求存在多种实现路径，应先说明权衡，再等待确认选择哪条实现路径
- 若发现当前代码逻辑与“视频重建主线”不一致，应明确指出

## 记忆更新规则
- 每次对话后，可根据新的项目理解更新当前 `AGENTS.md`
- 如有必要，可在相关子目录新增局部 `AGENTS.md`，用于记录该目录的职责、约束和注意事项
- 记忆内容应以“长期有效、可复用、能减少重复沟通”为标准
- 不写临时噪音信息，不记录一次性无价值细节

## 输出偏好
- 方案说明应优先围绕业务目标、实现思路、影响范围、风险点展开
- 修改说明应尽量清楚指出：
  - 为什么改
  - 改了什么
  - 对视频重建链路有什么帮助
- 如果未实际改代码，应明确说明“当前仅给出方案，尚未落盘修改”
